import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:minhaspautas/app/app_module.dart';

void main() {
  initModule(AppModule());
  // PautaController pauta;
  //
  setUp(() {
    //     pauta = AppModule.to.get<PautaController>();
  });

  group('PautaController Test', () {
    //   test("First Test", () {
    //     expect(pauta, isInstanceOf<PautaController>());
    //   });

    //   test("Set Value", () {
    //     expect(pauta.value, equals(0));
    //     pauta.increment();
    //     expect(pauta.value, equals(1));
    //   });
  });
}
